# Состояние среды разработки

> Документ Sprint 0. Подтверждает, что инструменты установлены, проект собирается и среда готова
> к разработке интерфейса в Sprint 1. Все значения получены выполнением команд 09.09.2026.

## 1. Версии инструментов

| Инструмент | Команда проверки | Установленная версия | Требование |
| --- | --- | --- | --- |
| Node.js | `node --version` | **v24.18.0** | LTS-линия — выполнено |
| npm | `npm --version` | **11.16.0** | входит в поставку Node.js |
| Git | `git --version` | **2.55.0.windows.2** | доступен из командной строки |
| npx | `npx --version` | **11.16.0** | запуск CLI без глобальной установки |
| Expo CLI | `npx expo --version` | **57.0.23** | локальный CLI из зависимостей проекта |
| TypeScript | из `package.json` | **~6.0.3** | строгий режим включён шаблоном |
| ОС | — | Windows 11 Pro 10.0.26200 | — |

## 2. Версии проекта

| Пакет | Версия |
| --- | --- |
| `expo` | ~57.0.21 |
| `react-native` | 0.86.3 |
| `react` | 19.2.3 |
| `expo-status-bar` | ~57.0.1 |
| `typescript` (dev) | ~6.0.3 |
| `@types/react` (dev) | ~19.2.2 |

Добавлено в Sprint 1 для запуска в браузере и снятия демонстрационного артефакта:

| Пакет | Версия |
| --- | --- |
| `react-dom` | 19.2.3 |
| `react-native-web` | ^0.21.2 |
| `@expo/metro-runtime` | ~57.0.15 |

Проект создан командой:

```bash
npx create-expo-app@latest worlddex --template blank-typescript
```

Установлено 467 пакетов. Шаблон `blank-typescript` выбран осознанно: он не тянет навигацию,
глобальное состояние и лишние библиотеки, что соответствует ограничениям Sprint 0.

## 3. Выполненные проверки

| Проверка | Команда | Результат |
| --- | --- | --- |
| Типизация | `npm run typecheck` (`tsc --noEmit`) | **OK**, код возврата 0, ошибок нет |
| Конфигурация проекта | `npm run doctor` (`expo-doctor`) | **21/21 проверок пройдено, проблем не обнаружено** |
| Доступность основного API | 6 HTTP-запросов к `api.worldbank.org` и `flagcdn.com` | все ответы 200, детали — в [product-concept.md](./product-concept.md#66-проверка-кандидата-выполнена-09092026) |
| Резервный источник | генерация `assets/data/countries.fallback.json` | 12 стран получены из реальных ответов API |

## 4. Целевая платформа и запуск

**Основная платформа:** Android. iOS и Web поддерживаются Expo и используются как вспомогательные
среды отладки.

| Способ запуска | Команда | Что требуется |
| --- | --- | --- |
| Физическое устройство | `npm start`, затем сканировать QR-код | приложение **Expo Go** на телефоне, телефон и ПК в одной сети Wi-Fi |
| Android-эмулятор | `npm run android` | Android Studio с настроенным AVD и переменной `ANDROID_HOME` |
| Браузер | `npm run web` | ничего дополнительно |

Рекомендуемый для этого проекта путь — **Expo Go на физическом Android-устройстве**: он не требует
установки Android Studio и даёт самый быстрый цикл обратной связи.

> Запуск на устройстве выполняется вручную и подтверждается скриншотом в отчёте. Скриншот
> экрана-заглушки — единственное визуальное подтверждение, требуемое в Sprint 0.

## 5. Чек-лист готовности к Sprint 1

- [x] Node.js LTS, npm и Git установлены, версии зафиксированы.
- [x] Проект React Native / Expo создан и проходит проверку типов.
- [x] `expo-doctor` не сообщает о проблемах конфигурации.
- [x] Целевая платформа выбрана, способ запуска описан.
- [x] Основной источник данных проверен и доступен без ключа.
- [x] Резервный источник данных лежит в репозитории.
- [x] `.gitignore` закрывает `node_modules` и `.env` — секреты в репозиторий не попадут.
- [ ] Запуск на физическом устройстве или эмуляторе выполнен и подтверждён скриншотом.

Единственный незакрытый пункт требует физического устройства и выполняется студентом вручную
перед защитой работы.

## 6. Metro и Hermes (Sprint 1)

**Metro** — сборщик JavaScript в React Native. Он превращает исходные модули проекта в один
бандл, отдаёт его устройству по HTTP и при изменении файла пересобирает только затронутый
модуль, что и делает возможным Fast Refresh.

**Hermes** — движок JavaScript, который исполняет этот бандл на устройстве. В проекте движок
явно не задан в `app.json`, поэтому действует значение по умолчанию для Expo SDK 57 — Hermes.
Проверено по манифесту, который Metro отдаёт клиенту: в адресе бандла присутствуют параметры
`transform.engine=hermes` и `transform.bytecode=1`, то есть код компилируется в байт-код Hermes.

Разница по задачам: Metro отвечает за сборку и доставку кода до запуска, Hermes — за выполнение
кода после запуска. Ошибка сборки приходит от Metro, ошибка времени выполнения — от Hermes.

**Fast Refresh проверен** 09.09.2026: при изменении поля `title` в `src/data/topics.ts` экран
обновился без полной перезагрузки приложения и без потери состояния.
