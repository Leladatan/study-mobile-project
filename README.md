# WorldDex — мобильный справочник стран мира

Один экран поиска, одна карточка на страну и все базовые факты из единого официального
источника — сопоставимые между странами и доступные без интернета.

> **Статус:** Sprint 1 — статическая витрина справочника. Приложение показывает 12 карточек
> стран, собранных из локальных fixtures на одном переиспользуемом компоненте `TopicCard`.
> Сетевые запросы, навигация и глобальное состояние подключаются в следующих спринтах.

![Витрина справочника: каталог и результат нажатия на карточку](assets/sprint-1.png)

| | |
| --- | --- |
| **Тематика** | Страны мира: административные, экономические и географические факты |
| **Стек** | React Native 0.86 + Expo SDK 57, TypeScript |
| **Платформа** | Android (основная), iOS и Web — вспомогательные |
| **Источник данных** | World Bank Indicators API v2 — без ключа и регистрации |
| **Дисциплина** | Разработка мобильных приложений, 5 семестр, Институт бизнеса и дизайна |

## Проблема

Чтобы собрать базовые факты об одной стране, пользователь открывает несколько статей: столица —
в одной, население — в другой, классификация по уровню дохода — в третьей. Данные из разных
источников ссылаются на разные годы, поэтому две страны нельзя честно сравнить. И ничего из этого
не работает без сети.

## Решение

WorldDex показывает **одну карточку на страну** с полным набором фактов из **одного официального
источника** — World Bank Open Data. Все страны описаны одинаковым набором полей за один и тот же
период. Последние загруженные данные и встроенный резервный набор позволяют пользоваться
справочником offline.

## Целевая аудитория

**Основной сегмент** — студенты 1–2 курса и старшеклассники, готовящие доклады и практические
работы по географии, экономике и обществознанию. Работают с телефона, часто в дороге, им нужен
проверяемый факт здесь и сейчас, со ссылкой на источник.

**Дополнительный сегмент** — участники квизов и олимпиад по географии.

Продукт **не рассчитан** на аналитиков, которым нужны временные ряды по десяткам показателей и
выгрузка в CSV.

## Основной пользовательский сценарий

Студент готовит доклад про Японию, до начала семинара 2 минуты:

1. Открывает приложение → видит список стран.
2. Вводит `яп` / `jap` в поиск → список фильтруется по мере ввода.
3. Нажимает «Japan» → открывается карточка страны.
4. Видит флаг, столицу, регион, уровень дохода, население с годом наблюдения и координаты.
5. Возвращается назад → поисковый запрос сохранён.

Нужный факт получен менее чем за 30 секунд, без перехода в браузер. Если сети нет, шаг 1 отдаёт
данные из резервного источника с явной плашкой об этом.

## Границы MVP

**Входит:** список стран, поиск по названию, карточка страны, флаг, состояния загрузки и ошибки,
работа без сети.

**Не входит в первую версию:** авторизация, карта, графики динамики по годам, сравнение стран,
полная локализация, push-уведомления. Обоснование каждого отказа —
в [docs/product-concept.md](docs/product-concept.md#5-границы-mvp).

## Источник данных

| | |
| --- | --- |
| API | World Bank Indicators API v2 |
| Документация | https://datahelpdesk.worldbank.org/knowledgebase/articles/889392-about-the-indicators-api-documentation |
| Базовый URL | `https://api.worldbank.org/v2/` |
| Доступ | HTTP GET, `?format=json`, **без API-ключа и регистрации** |
| Флаги | https://flagcdn.com — статический CDN по кодам ISO 3166-1 alpha-2 |
| Резерв | [`assets/data/countries.fallback.json`](assets/data/countries.fallback.json) — 12 стран, сгенерированы из реальных ответов API |
| Лицензия данных | CC BY-4.0, World Bank Open Data |

Проверка кандидата выполнена 09.09.2026: все запросы вернули 200, API отдаёт 295 записей,
из которых 217 — реальные страны и территории. Полный протокол проверки, включая причину отказа
от REST Countries, — в [docs/product-concept.md](docs/product-concept.md#66-проверка-кандидата-выполнена-09092026).

## Состояние среды разработки

| Инструмент | Версия | Статус |
| --- | --- | --- |
| Node.js | v24.18.0 | LTS |
| npm | 11.16.0 | ok |
| Git | 2.55.0.windows.2 | ok |
| Expo CLI | 57.0.23 | ok |
| `tsc --noEmit` | — | **проходит без ошибок** |
| `expo-doctor` | — | **21/21 проверок пройдено** |

Подробнее — в [docs/environment.md](docs/environment.md).

## Запуск

```bash
npm install     # если репозиторий склонирован заново
npm start       # Expo Go: сканировать QR-код с телефона
npm run android # Android-эмулятор (нужен Android Studio)
npm run web     # браузер
npm run typecheck
npm run doctor
```

## Структура проекта

```
worlddex/
├── App.tsx                            точка сборки экрана
├── index.ts                           точка входа Expo
├── app.json                           конфигурация приложения
├── src/
│   ├── types/topic.ts                 тип Topic и справочники названий
│   ├── data/topics.ts                 локальные fixtures, 12 объектов
│   ├── components/TopicCard.tsx       переиспользуемая карточка
│   ├── screens/CatalogScreen.tsx      экран каталога
│   ├── lib/format.ts                  форматирование чисел
│   └── theme.ts                       цвета, отступы, радиусы
├── assets/
│   ├── data/countries.fallback.json   резервный источник данных
│   └── sprint-1.png                   демонстрационный артефакт Sprint 1
├── docs/
│   ├── product-concept.md             концепция, аудитория, MVP, кандидат API
│   ├── backlog.md                     product backlog, 9 историй
│   ├── environment.md                 версии инструментов и готовность среды
│   └── git-workflow.md                ветка, pull request и тег спринта
├── reports/                       отчёт о практической работе № 0
└── CHANGELOG.md
```

## Документация

- [Концепция продукта](docs/product-concept.md) — проблема, аудитория, сценарий, границы MVP, API.
- [Product backlog](docs/backlog.md) — 9 историй, критерии приёмки US-1.
- [Среда разработки](docs/environment.md) — версии, проверки, чек-лист готовности.
- [Git-процесс](docs/git-workflow.md) — ветка, pull request, тег `sprint-0`.
- [Отчёт № 0](reports/REPORT-00.md) — отчёт о практической работе.

## Как устроена витрина

`Topic` — тип основной сущности справочника: обязательные `id` и `title`, содержательное
`description`, необязательное `imageUrl` и поля предметной области — столица, регион, уровень
дохода, население с годом наблюдения и коды ISO.

`TopicCard` — переиспользуемый компонент. Он не знает, откуда пришли данные: получает готовый
объект `Topic` через props и сообщает о нажатии через типизированный callback
`onPress: (topic: Topic) => void`. Необязательный prop `featured` включает укрупнённое
оформление — им выделена первая карточка каталога. Если у объекта нет `imageUrl`, вместо флага
рисуется заглушка с кодом страны, и вёрстка не ломается.

`CatalogScreen` перебирает fixtures и переиспользует `TopicCard`; разметка карточки нигде
не дублируется. Нажатие обновляет строку «Выбранная карточка» над списком.

## Дальше

Sprint 2 — интерактивный каталог: `FlatList`, управляемый поиск, фильтр по региону,
выбор карточек со счётчиком и пользовательский hook `useCatalog`.
